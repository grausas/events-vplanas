// styles
import { Wrapper, SliderDiv } from "./DateSlider.style";

const TimeSlider = ({ id }) => {
  return (
    <Wrapper>
      <SliderDiv id={id} />
    </Wrapper>
  );
};

export default TimeSlider;
